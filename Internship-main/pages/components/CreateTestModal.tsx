import React from 'react';

// Stub component for CreateTestModal (not needed in module tester)
const CreateTestModal: React.FC<any> = (props) => {
  return (
    <div className="p-4 text-center text-gray-500 dark:text-gray-400">
      <p>Create Test Modal - Not available in module tester</p>
    </div>
  );
};

export default CreateTestModal;
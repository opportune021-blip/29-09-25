import React from 'react';

// Stub component for InteractionHeatmap (not needed in module tester)
const InteractionHeatmap: React.FC<any> = (props) => {
  return (
    <div className="p-4 text-center text-gray-500 dark:text-gray-400">
      <p>Interaction Heatmap - Not available in module tester</p>
    </div>
  );
};

export default InteractionHeatmap;